@model FlightWebApplication.Models.SearchFlightData
@{
    ViewBag.Title = "Search Flight Details";
}

<h2><b>@ViewBag.Title</b></h2>
<h3>@ViewBag.Message</h3>



@*<form asp-controller="Movies" asp-action="Index" method="get">
        <p>

            <select asp-for="MovieGenre" asp-items="Model.Genres">
                <option value="">All</option>
            </select>

            Title: <input type="text" asp-for="SearchString" />
            <input type="submit" value="Filter" />
        </p>
    </form>*@

<table class="table">
    <thead>
        <tr>
            <th>
                Flight Number
            </th>
            <th>
                @*@Html.DisplayNameFor(model => model.Movies[0].ReleaseDate)*@
            </th>
            <th>
                @*@Html.DisplayNameFor(model => model.Movies[0].Genre)*@
            </th>
            <th>
                @*@Html.DisplayNameFor(model => model.Movies[0].Price)*@
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @if (Model.flightDatas != null)
        {
            foreach (var item in Model.flightDatas)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.FlightNumber)
                    </td>
                    <td>
                        @*@Html.DisplayFor(modelItem => item.ReleaseDate)*@
                    </td>
                    <td>
                        @*@Html.DisplayFor(modelItem => item.Genre)*@
                    </td>
                    <td>
                        @*@Html.DisplayFor(modelItem => item.Price)*@
                    </td>
                    <td>
                        @*<a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                            <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                            <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>*@
                    </td>
                </tr>
            }

        }

    </tbody>
</table>



@{
    using (Html.BeginForm("Index", "Search", FormMethod.Post))
    {
        @Html.TextBoxFor(m => m.Flight_Number)
        @*@Html.TextBoxFor(m => m.Destination_Name)
        @Html.TextBoxFor(m => m.LowestFlightPrice)
        @Html.TextBoxFor(m => m.Flight_Number)*@
        <input type="submit" value="Search" />
    }
}

